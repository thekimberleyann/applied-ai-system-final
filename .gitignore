__pycache__/
.venv/
.venv_new/
# Native-Windows venv. The repo lives on C:, so running it with Windows Python
# avoids WSL's 9P filesystem translation entirely, which is the single biggest
# speed factor for launching Streamlit here.
.venv-win/
.pytest_cache/
.DS_Store

# Local-only working files (never upload)
PLAN.local.md
ai_log.local.md
*.local.md

# Secrets: the live-mode Gemini key lives here and must NEVER be committed.
# .env.example (a keyless template) IS tracked so others know what to set.
.env
